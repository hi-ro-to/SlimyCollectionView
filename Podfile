# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'SampleSlimyCollectionView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SampleSlimyCollectionView
  pod 'SDWebImage', '~> 4.0'
end

target 'SlimyCollectionView' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SlimyCollectionView
  pod 'SDWebImage', '~> 4.0'
end
